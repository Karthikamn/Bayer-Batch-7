name: Context

on: workflow_dispatch

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - run: exit 1
 test:
  needs: build
  runs-on: ubuntu-latest
  steps:
   - run: echo"test"
 deploy:
  needs: [ build , test] 
  runs-on: ubuntu-latest
  steps:
   - run: echo"deploy"
  
  
